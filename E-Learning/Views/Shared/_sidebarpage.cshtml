
<nav class="navbar navbar-expand-lg shadow navbar-inverse navbar-fixed-top navbar-light ">
    <div class="container-fluid">

        <button type="button" id="sidebarCollapse" class="btn btn-info">
            <i class="fas fa-align-left"></i>
            
        </button>
        <button class="btn btn-dark d-inline-block d-lg-none ml-auto" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-align-justify"></i>
        </button>

        <div class="collapse navbar-collapse rounded-circle " id="navbarSupportedContent">
            @if (User.IsInRole("Tutor"))
            {

                @RenderPage("~/Views/Shared/_TutorImage.cshtml")
            }
            else if (User.IsInRole("Student"))
            {
                @RenderPage("~/Views/Shared/_StudentImage.cshtml")
            }

        </div>
    </div>
</nav>
    